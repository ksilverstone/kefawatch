spring:
  flyway:
    enabled: true
  datasource:
    hikari:
      maximum-pool-size: 5

kefawatch:
  jwt:
    secret: test-secret-must-be-long-enough-for-hs256-algorithm!!

logging:
  level:
    org.springframework.security: WARN
